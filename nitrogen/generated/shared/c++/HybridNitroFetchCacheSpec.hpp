///
/// HybridNitroFetchCacheSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `CachedPrefetchResponse` to properly resolve imports.
namespace margelo::nitro::nitrofetch { struct CachedPrefetchResponse; }

#include "CachedPrefetchResponse.hpp"
#include <optional>
#include <string>

namespace margelo::nitro::nitrofetch {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroFetchCache`
   * Inherit this class to create instances of `HybridNitroFetchCacheSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroFetchCache: public HybridNitroFetchCacheSpec {
   * public:
   *   HybridNitroFetchCache(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroFetchCacheSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroFetchCacheSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroFetchCacheSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::optional<CachedPrefetchResponse> getCachedPrefetch(const std::string& key, double maxAgeMs) = 0;
      virtual bool isPrefetchPending(const std::string& key) = 0;
      virtual void clearAll() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroFetchCache";
  };

} // namespace margelo::nitro::nitrofetch
