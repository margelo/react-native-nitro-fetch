///
/// WebSocketReadyState.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitrofetchwebsockets {

  /**
   * An enum which can be represented as a JavaScript union (WebSocketReadyState).
   */
  enum class WebSocketReadyState {
    CONNECTING      SWIFT_NAME(connecting) = 0,
    OPEN      SWIFT_NAME(open) = 1,
    CLOSING      SWIFT_NAME(closing) = 2,
    CLOSED      SWIFT_NAME(closed) = 3,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitrofetchwebsockets

namespace margelo::nitro {

  // C++ WebSocketReadyState <> JS WebSocketReadyState (union)
  template <>
  struct JSIConverter<margelo::nitro::nitrofetchwebsockets::WebSocketReadyState> final {
    static inline margelo::nitro::nitrofetchwebsockets::WebSocketReadyState fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("CONNECTING"): return margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CONNECTING;
        case hashString("OPEN"): return margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::OPEN;
        case hashString("CLOSING"): return margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CLOSING;
        case hashString("CLOSED"): return margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CLOSED;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum WebSocketReadyState - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::nitrofetchwebsockets::WebSocketReadyState arg) {
      switch (arg) {
        case margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CONNECTING: return JSIConverter<std::string>::toJSI(runtime, "CONNECTING");
        case margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::OPEN: return JSIConverter<std::string>::toJSI(runtime, "OPEN");
        case margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CLOSING: return JSIConverter<std::string>::toJSI(runtime, "CLOSING");
        case margelo::nitro::nitrofetchwebsockets::WebSocketReadyState::CLOSED: return JSIConverter<std::string>::toJSI(runtime, "CLOSED");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert WebSocketReadyState to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("CONNECTING"):
        case hashString("OPEN"):
        case hashString("CLOSING"):
        case hashString("CLOSED"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
